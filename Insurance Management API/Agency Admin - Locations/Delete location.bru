meta {
  name: Delete location
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/agency-admin/locations/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
